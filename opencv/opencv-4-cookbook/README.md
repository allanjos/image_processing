# OpenCV Cookbook study

## Build

To build the Visual Studio project, add the OpenCV build for Windows in folder `third/opencv` inside the solution folder.

Images are to copied to `resources` folder.